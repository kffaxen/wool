#include "wool.h"
#include "nqueens.c"

#test wool1
    ck_assert_msg( CALL( find_queens, 8 ) == 92, "nqueens(8) returned the wrong answer");

#main-pre
    wool_init(0, NULL);

#main-post
    wool_fini();
    return nf == 0 ? 0 : 1;